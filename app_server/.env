# 环境配置文件
# 指定当前使用的环境配置
# 该文件可以提交到Git，方便团队协作时指定默认环境

# ==================== 应用环境配置 ====================
# 应用环境: local(本地开发) / develop(开发服务器) / production(生产环境)
# 系统会自动加载对应的 .env.{APP_ENV} 配置文件
APP_ENV=develop
