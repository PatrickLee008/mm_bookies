server:
  port: 8082
  servlet:
    context-path:
  tomcat:
    uri-encoding: UTF-8
    basedir: /www/wwwroot/web_manager_api/rundir
    # 连接保活配置（防止 EOFException）
    connection-timeout: 60000 # 60秒连接超时
    keep-alive-timeout: 60000 # 60秒保活超时
    max-keep-alive-requests: 100 # 最大保活请求数
    accesslog:
      pattern: common
      enabled: true
      directory: logs
      prefix: jeeplus_access_log
      suffix: .log
      request-attributes-enabled: true
      rename-on-rotate: true
    max-http-form-post-size: 200MB
  jetty:
    max-http-form-post-size: 20000000B
logging:
  level:
    root: INFO
    #    org.flowable: DEBUG
    com.jeeplus: INFO
spring:
  servlet:
    multipart:
      maxFileSize:  1000MB
      maxRequestSize: 1000MB
  devtools:
    restart:
      enabled: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    dynamic:
      p6spy: false # 性能分析插件(有性能损耗 生产环境请关闭)
      primary: master
      datasource:
        master:
          username: root
          password: zaq12WSX
          url: jdbc:mysql://127.0.0.1:3306/onex2_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&nullCatalogMeansCurrent=true&serverTimezone=GMT%2B8
          driver-class-name: com.mysql.cj.jdbc.Driver
          #          driver-class-name: com.p6spy.engine.spy.P6SpyDriver

          #oracle database settings
          #          username: monitor
          #          password: monitor
          #          url: jdbc:oracle:thin:@localhost:1521:helowin
          #          driver-class-name: oracle.jdbc.driver.OracleDriver


          ##mssql database settings
          #          username: sa
          #          password: yourStrong(!)Password
          #          driver-class-name: net.sourceforge.jtds.jdbc.Driver
          #          url: jdbc:jtds:sqlserver://localhost:1433;SelectMethod=cursor;DatabaseName=jeeplus
          #          SelectMethod: cursor


          ##pg database settings
  #              username: postgres
  #              password: password
  #              driver-class-name: org.postgresql.Driver
  #              url: jdbc:postgresql://localhost:54321/jeeplus-vue-pg
  #              testSql: SELECT 'x'
  # if you need use pg database , please enable this config.
  #    jpa:
  #      properties:
  #        hibernate:
  #          jdbc:
  #            lob:
  #              non_contextual_creation: true
  mvc.servlet.load-on-startup: 1
  jmx:
    enabled: false
  # quartz任务配置
  quartz:
    job-store-type: jdbc
    properties:
      org:
        quartz:
          scheduler:
            instanceName: clusteredScheduler
            instanceId: AUTO
          jobStore:
            selectWithLockSQL: SELECT * FROM {0}LOCKS UPDLOCK WHERE LOCK_NAME = ?
            class: org.springframework.scheduling.quartz.LocalDataSourceJobStore
            tablePrefix: QRTZ_
            isClustered: false # 打开集群配置
            clusterCheckinInterval: 2000 # 设置集群检查间隔20s
            useProperties: false
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadCount: 10
            threadPriority: 5
            threadsInheritContextClassLoaderOfInitializingThread: true
  # redis配置
  data:
    redis:
      host: localhost
      port: 6379
      expireTime: 3600000 #单位秒
      database: 3
#  jackson:
#    default-property-inclusion: NON_NULL
#    time-zone: GMT+8
#    date-format: yyyy-MM-dd HH:mm:ss
flowable:
  async-executor-activate: false #关闭定时任务JOB
  #  将databaseSchemaUpdate设置为true。当Flowable发现库与数据库表结构不一致时，会自动将数据库表结构升级至新版本。
  database-schema-update: true
  check-process-definitions: false
  process-definition-location-prefix: classpath:/processes/
  activity-font-name: 宋体
  label-font-name: 宋体
  annotation-font-name: 宋体
  # 关闭各个模块生成表，目前只使用工作流基础表
  idm:
    enabled: false
  cmmn:
    enabled: false
  dmn:
    enabled: false
  app:
    enabled: false
#mybatis-plus配置
mybatis-plus:
  config-location: classpath:/mybatis/mybatis-config.xml
  mapper-locations:
    - classpath*:com/jeeplus/**/*Mapper.xml
    - classpath:/META-INF/admin-mybatis-mappings/*.xml
    - classpath:/META-INF/modeler-mybatis-mappings/*.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.jeeplus.**.domain
  configuration-properties:
    prefix:
    boolValue: TRUE
    blobType: BLOB
  type-handlers-package: com.jeeplus.core.mapper
  global-config:
    db-config:
      logic-delete-field: delFlag # 全局逻辑删除字段名
      logic-delete-value: 1 # 逻辑已删除值
      logic-not-delete-value: 0 # 逻辑未删除值

# springdoc-openapi项目配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
#  group-configs:
#    - group: 'default'
#      paths-to-match: '/**'
#      packages-to-scan: com.jeeplus
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn

#cas服务端的地址
cas:
  server-url-prefix: https://www.cainiao.com:8443/cas
#wps配置
wps:
  appid: 3f86c4255efd4e9c9d3ce6554e0fdf0d
  appsecret: 4bbc464724dc4351a93a70c95856197f
  download_host: http://demo1.jeeplus.org/jeeplus-vue
  domain: https://wwo.wps.cn
  downloadCallbackPath: /usr/local/wps
  webctx: /jeeplus-vue

#演示模式
demoMode: false

#LRC运行参数
appRun:
  debug:  false
  memberId: 1901575145475538946

#LRC运行参数
app:
  pay:
    payApi: http://pay.okbetmm.com
    payAppid: a30786d6303331636ac4cbcaf8673bf7
    paySecret: 1mnsht8OLIGz2xfokdnGew==
    payoutCallbackUrl: http://ag.onex2.com/api
  vendor:
    openApi: http://localhost:9101
  config:
    appApiUrl: http://m.onex2.com/s

#oss配置
config:
  accessory:
    #local, aliyun, minIO
    type: aliyun
    baseDir: local
    local:
      location: /www/wwwroot/web_manager_api/filedir
    aliyun:
      endpoint: oss-ap-southeast-1.aliyuncs.com
      accessKeyId: LTAI5tCj9rxJfZsb5mjd3UPh
      accessKeySecret: Zd0sqWI84XWAXJalf3d6SJJ4nWiKJP
      bucketName: onex2
    minIO:
      endpoint:
      accessKey:
      secretKey:
      bucketName:
#上传文件类型配置
file:
  allowedType: file  # 允许上传的文件类型， all, file ,image, audio, video, office
  extensions:
    all: all       # 允许上传所有类型文件
    file: 7z,aiff,asf,avi,bmp,csv,doc,docx,fla,flv,gif,gz,gzip,jpeg,jpg,mid,mov,mp3,mp4,mpc,mpeg,mpg,ods,odt,pdf,png,ppt,pptx,pxd,qt,ram,rar,rm,rmi,rmvb,rtf,sdc,sitd,swf,sxc,sxw,tar,tgz,tif,tiff,txt,vsd,wav,wma,wmv,xls,xlsx,zip       # 只允许上传安全文件（linux系统非可执行）
    image: gif,jpg,jpeg,bmp,png     # 只允许上传图片
    audio: CD,OGG,MP3,ASF,WMA,WAV,MP3PRO,RM,REAL,APE,MODULE,MIDI,VQF    # 只允许上传音频
    video: AVI,WMV,RM,RMVB,MPEG1,MPEG2,MPEG4(MP4),3GP,ASF,SWF,VOB,DAT,MOV,M4V,FLV,F4V,MKV,MTS,TS     # 只允许上传视频
    office: txt,xls,xlsx,xlsm,xltx,xltm,xlsb,xlam,doc,docx,docm,dotx,dotm,ppt,pptx,pptm,ppsx,ppsm,potx,potm,ppam     # 只允许上传office文件




#===============================#
#====== license 信息 ================#
#===============================#
# 开发者全局信息配置，优先级高于数据库中的配置，如果不启用将采用数据库中配置
devtool:
  enabled: false      # 是否启用本地配置, true/false
  projectPath: /xx          #后端生成路径
  frontPath: /xx    # 前端生成路径
  packageName: com.jeeplus    # 默认包名
  author: xx     # 默认作者
# 产品授权
productId:
license:

#token过期时间
jwt.accessToken.expireTime: 86400  # 86400 ，24*3600* 24小时，单位秒

# JWT配置 - 与Python后端保持一致
jwt:
  secret: abc123
  expiration: 43200000 # 12小时 (与Python backend一致: 60*60*12*1000)

# WebSocket配置
websocket:
  max-text-message-size: 65536 # 64KB - 最大文本消息大小
  max-binary-message-size: 65536 # 64KB - 最大二进制消息大小
  max-session-idle-timeout: 1800000 # 30分钟 - 会话空闲超时时间(毫秒)
  heartbeat-interval: 30000 # 30秒 - 心跳检测间隔(毫秒)

# 游戏回调配置
game:
  callback:
    secret: mrjuCogIY7F8UW8gGxm  # AWC游戏平台回调验证密钥（与AWC平台约定的key）





